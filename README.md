# Diabetes Prediction Project 🩺

## Overview
This project uses Machine Learning to predict whether a patient has diabetes based on diagnostic measurements (like glucose level, BMI, age, etc.).

## 📂 Project Structure
- `data/`: Contains the dataset (not included in repo for privacy).
- `main.py`: The main script to train the model.
- `requirements.txt`: List of Python dependencies.

## 🚀 How to Run
1. **Clone the repository:**
   ```bash
   git clone git@github.com:MeemansaKapur/ml-project-diabetes.git